# pnpm settings. auto-install-peers is on by default since pnpm 8 but kept
# explicit for clarity. strict-peer-dependencies kept off because the
# Next.js + React 19 ecosystem still has a few mismatched peer ranges.
auto-install-peers=true
strict-peer-dependencies=false
